<h1>Categories</h1>

<table>
  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= link_to category.name, category %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "Back", expenses_path %>