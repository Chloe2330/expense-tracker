<h1><%= "Expense #{@expense.id}" %></h1>

<div>
    <%= link_to "Back", expenses_path %>
    <%= link_to "Edit", edit_expense_path(@expense) %>
</div>

<br>

<%= button_to "Delete", @expense, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
